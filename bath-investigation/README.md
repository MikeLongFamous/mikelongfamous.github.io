# Bath Investigation — W-319 public corpus export

Read-only GitHub Pages surface for exactly six sanitized records already proven in W-311. This directory contains no authentication, cookies, write endpoint, paid inference, private Drive identifiers, MIKE OS paths, confidential material, or broader investigation corpus.

The deterministic `QuickBooks` query is pre-rendered at `search/quickbooks/index.html`; agents do not need JavaScript. `corpus.json`, `agent-index.json`, `robots.txt`, `sitemap.xml`, `manifest.sha256`, and the privacy review are static public artifacts.

This export does not complete W-311, validate a broader corpus, or constitute a final investigative report.
